export enum SubscriptionPlan {
  FREE = 'free',
  CLASSIC = 'classic',
  PRO = 'pro',
  MAX = 'max',
}

export enum SubscriptionStatus {
  ACTIVE = 'active',
  EXPIRED = 'expired',
  CANCELLED = 'cancelled',
  PENDING = 'pending',
}
